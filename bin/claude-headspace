#!/Users/samotage/dev/otagelabs/claude_headspace/venv/bin/python3
"""
Claude Headspace CLI entry point.

Launches Claude Code sessions with monitoring integration.
"""

import sys
from pathlib import Path

# Add src to path for development usage
src_path = Path(__file__).parent.parent / "src"
if src_path.exists():
    sys.path.insert(0, str(src_path))

from claude_headspace.cli import main

if __name__ == "__main__":
    sys.exit(main())
